syntax = "proto3";
package auth;

service AuthService {
  rpc RegisterClient (RegisterRequest)       returns (AuthResponse);
  rpc LoginClient    (LoginRequest)          returns (AuthResponse);
  rpc LoginUser      (LoginRequest)          returns (AuthResponse);
  rpc ValidateToken  (ValidateRequest)       returns (ValidateResponse);
  rpc CreateToken    (CreateTokenRequest)    returns (CreateTokenResponse);
}

message RegisterRequest {
  string email    = 1;
  string password = 2;
  string name     = 3;
}

message LoginRequest {
  string email    = 1;
  string password = 2;
}

message AuthResponse {
  string token                = 1;
  string role                 = 2;
  bool   success              = 3;
  string message              = 4;
  repeated string permissions = 5;
}

message ValidateRequest {
  string token = 1;
}

message ValidateResponse {
  string user_id              = 1;
  string role                 = 2;
  bool   valid                = 3;
  repeated string permissions = 4;
}

message CreateTokenRequest {
  string user_id = 1;
  string role    = 2;
}

message CreateTokenResponse {
  string token   = 1;
  bool   success = 2;
  string message = 3;
}

