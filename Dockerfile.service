FROM node:22-alpine

ARG SERVICE_PATH

WORKDIR /app

# Copy package metadata and local shared proto package first for better layer caching.
COPY ${SERVICE_PATH}/package*.json ./
COPY proto-contracts /proto-contracts

RUN npm install --omit=dev

COPY ${SERVICE_PATH}/src ./src

CMD ["node", "src/server.js"]
